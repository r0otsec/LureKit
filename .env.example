# === LureKit Environment Configuration ===

# Fully-qualified subdomains
PHISH_DOMAIN=phish.yourdomain.com
REDIRECT_DOMAIN=login.yourdomain.com

# Mailgun SMTP credentials
MAILGUN_SMTP_USER=postmaster@yourdomain.com
MAILGUN_SMTP_PASS=your-mailgun-smtp-password

# GoPhish admin panel port (defaults to 3333)
GOPHISH_ADMIN_PORT=3333

# GeoIP filter region (ISO country code, e.g., GB, US, etc.)
ALLOWED_COUNTRY=GB

# DigitalOcean API token (used by Terraform)
DO_TOKEN=your-digitalocean-token
